<%= render 'shared/navbar' %>

<div class="container">
  <h2 class='text-center mt-5 p-2'>CHATROOMS</h2>

  <% @chatrooms.each do |chatroom| %>
    <%= link_to "#{chatroom.name}", chatroom_path(chatroom) %>
  <% end %>
</div>
